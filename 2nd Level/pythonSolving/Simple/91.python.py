# Get Density and Volume from the user
# calculate the mass
# display the result

density = float(input("Enter a density: "))
volume = float(input("Enter a voume: "))

mass = density * volume

print(f"The mass of {density} X {volume} is = {mass}")